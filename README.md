# esp32s3_dual_foc_gp

This project is for esp32-S3 and ESP-IDF framework. It implements a USB game controller where two axes are controlled by two BLDC motors coupled with AS5600 Magnetic Encoders, using the motor force for haptic feedback. The motors are connected to the ESP32-S3 pins via DRV8833 drivers.
